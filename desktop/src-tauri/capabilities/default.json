{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop shell permissions for the bundled loading and settings UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
