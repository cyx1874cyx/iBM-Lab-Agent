# v0.4.3 发布说明与 DSH 兼容性验证

> `harness.lock.json` 的 `notes` 指向本文件作为"兼容性与验证记录"的落点。
> 本文所有结论均来自本版本仓库内的实测命令，命令与结果一并列出，便于复现。

## 1. 本版本锁定

| 层 | 版本 | 记录位置 |
|---|---|---|
| iBM Lab Agent | `0.4.3` | `package.json`、`runtime/versions.env`、`desktop/docs/release-manifest.json` |
| DeepSeek Harness（CLI 与包集） | `0.1.5-rc.1` | `harness.lock.json`、`runtime/versions.env` |
| 运行时补丁基线 | `DSH_AGENT_LOOP_SHA256=257eb83c00a05ee068e9f4ba80ca71ab94e3a1275d24b7a0cf5038ff23dd0fd8` | `runtime/versions.env` |
| Windows 安装包 | `iBM Lab Agent_0.4.3_x64-setup.exe`（221,846,674 B，SHA256 `eabc0366b961a34f28e4e3a805bdb936d2fb9c8b24b5baa9fa5c79bd03859801`） | `Code/Releases/v0.4.3/` |

## 2. DSH `0.1.5-rc.1` 兼容性结论

| # | 检查项 | 判据 | 结果 |
|---|---|---|---|
| 1 | 是否为最新已发布 DSH | `@deepseek-ai/dsh` dist-tags：`latest=0.1.5-rc.1`、`next=0.1.5-rc.1`；`0.1.5-rc.1` 发布 `2026-09-10T03:12:53Z` | ✅ 是当前最高已发布版本 |
| 2 | 锁定 ↔ 安装树一致 | `harness.lock.json` 的 15 个包 + `runtime/launcher/package.json` 的 `@deepseek-ai/dsh` 逐项比对 `node_modules` | ✅ 16/16 一致 |
| 3 | 运行时补丁锚点未失效 | `runtime/versions.env` 的 `DSH_AGENT_LOOP_SHA256` vs 实测 `node_modules/@deepseek-ai/dsh-agent-loop/lib/index.js` 的 sha256 | ✅ 逐字节一致（仍为 pristine 0.1.5 代码） |
| 4 | 浏览器注入面存在 | `package.json` `dsh.client.inject` 的 6 个模块在 0.1.5 均有 `./client` 导出 | ✅ 全部存在 |
| 5 | 客户端 API 迁移 | 0.1.5 起会话切换由 `uiWorkspace` 承载（`dsh-client-ui-workspace` 提供 `connectWorkspace` / `openSession`） | ✅ 客户端已迁移 |
| 6 | 构建产物与源码一致 | `node scripts/build-client.mjs --check` | ✅ 通过（`client/index.js` 291676 字节） |
| 7 | preset loader 与导出面一致 | `node scripts/check-preset-exports.mjs` | ✅ 6 个 preset loader 条目 ⊆ `dsh-lab-agent` exports |

### 2.1 0.1.5 的服务面变化（本次迁移依据）

在 `0.1.5-rc.1` 安装树中实测到的服务注册者：

| 服务名 | 注册方 | 用途 |
|---|---|---|
| `sessions` | `@deepseek-ai/dsh-session` | 会话数据（仍存在） |
| `workspaces` | `@deepseek-ai/dsh-api-workspace-controller` | 工作区增删改查（仍存在） |
| `uiWorkspace` | `@deepseek-ai/dsh-client-ui-workspace` | **新增**：`connectWorkspace` / `openSession` |
| `uiSession` | `@deepseek-ai/dsh-client-ui-session` | 会话 UI 状态 |

因此客户端把 `ctx.sessions.open(...)` 改为 `ctx.uiWorkspace.openSession(...)`、
`ctx.workspaces.connectWorkspace(...)` 改为 `ctx.uiWorkspace.connectWorkspace(...)`，
并在 `ctx.inject([...])` 中显式加入 `"uiWorkspace"`。`sessions` / `workspaces` 仍在注入面上，
因为它们仍分别承担会话数据与工作区管理。

## 3. 验证记录

| 门禁 | 命令 | 结果 |
|---|---|---|
| 单元 + 集成 | `node --test "tests/unit/*.test.mjs" "tests/integration/*.test.mjs"` | **356 / 356 通过** |
| DSH 兼容性专项 | `node --test tests/integration/harness-compatibility.test.mjs` | **2 / 2 通过** |
| 回归套件 | `node scripts/regression/run.mjs` | **11 / 11 通过**（含 `harness-pin`：15 packages match `harness.lock.json`） |
| 客户端一致性 | `node scripts/build-client.mjs --check` | 通过 |
| preset 导出 | `node scripts/check-preset-exports.mjs` | 通过 |
| 浏览器验收 | `node --test "tests/browser/*.test.mjs"` | **7 / 7 通过** |
| 桌面安装包全流水线 | `desktop/scripts/build-windows-release.ps1`（干净提交） | **11 / 11 阶段通过**，`publishable=true`；`tauri-nsis` 14m31s |

安装包（`build-windows-release.ps1`）各阶段实测耗时：client-bundle-check 0.2s · tests 9.6s ·
regression 1.8s · preset-exports 0.1s · browser-ketcher 19.2s · lint 2.0s · bundled-python 5m23s ·
prepare-runtime 3.2s · verify-runtime 30.6s · tauri-nsis 14m31s · verify-installer 21.4s。

产物另做三层校验：7z 内容清点（dsh 29,844 / python 14,505 / plugin 2,040 条目、
`python\dist\python.exe`、`mnova_mcp`、`origin_mcp`、`bridge.qs`、ketcher 与 pdf-viewer 静态块均在）；
包内 `ibm-lab-desktop.exe` 的 `FileVersion=0.4.3`（证明非旧缓存）；
归档副本与构建产物 sha256 逐字节一致。

`tests/integration/harness-compatibility.test.mjs` 为本版本新增，把"锁定的 DSH 与声明面一致"
固化为可执行断言：

- 启动器与 `devDependencies` 的 `@deepseek-ai/dsh` 必须等于 `harness.lock.json` 的 `cli`；
- 所有声明的浏览器模块与 peer 依赖必须在安装树中解析得到，且浏览器模块必须有 `./client` 导出；
- `@deepseek-ai/dsh-agent-loop` 的 sha256 必须等于 `runtime/versions.env` 的锁定值，
  fake-invoke 补丁可应用、幂等、且可无损回滚。

## 4. 本版本修复

1. **客户端 DSH 0.1.5 服务迁移**：`client/src/apply.js` / `client/index.js` 迁移到 `uiWorkspace`。
2. **注入面断言同步**（`tests/unit/harness-surface.test.mjs`）：此前套件为 356 项 / 354 通过，
   失败的两项均为注入面断言未随客户端改动更新（`:281` 正则、`:356` `deepStrictEqual`）。
   现已同步 `"uiWorkspace"`，套件转全绿。
3. **Windows 上 Harness 定位失败**（`src/harness-root.js`）：原实现只用 `which` 查 PATH，
   而 Windows 无 `which`，因此在未设置 `DSH_HARNESS_NODE_MODULES` 的 Windows 环境里
   `harness-pin` 回归用例与 `scripts/dev-link.mjs` 必然定位失败。现改为：
   - win32 使用 `where`，POSIX 仍用 `which`；
   - 追加 npm 全局 shim 布局（`%APPDATA%\npm\dsh.cmd` → `%APPDATA%\npm\node_modules`）；
   - 追加回退：直接解析本插件自身依赖到的 `@deepseek-ai/dsh`，取其 `node_modules`。
   修复后 `harness-pin` 在未设环境变量的 Windows 上通过（实测 15 packages match）。
4. **`.gitignore`**：`.tmp-*.json` 放宽为 `.tmp-*`，避免升级/排查用的临时日志与探针脚本入库。
5. **打包 Web 冒烟门禁在 0.1.5 上必然失败**（`desktop/scripts/verify-package.ps1`）：
   DSH `0.1.5-rc.1` 对 Web 面**强制进程 token**——不带 token 的 `GET /` 返回 **401**，
   而 `dsh web` 没有关闭校验的开关（0.4.2 所用的 `0.1.1-rc.2` 完全不校验，所以只有升级后才暴露）。
   该阶段轮询回环地址并要求 2xx–3xx 响应，因此在 0.1.5 上**永远不可能通过**，
   整个 Windows 发布在 `verify-runtime` 阶段中止。现改为从启动横幅
   （process-token URL line，DSH 唯一的 token 暴露点）解析 token，并附加到每一个冒烟请求：
   健康探测、capture handoff 路由、Ketcher 首页及其引用的静态资源。
6. **浏览器验收随机失败**（`tests/browser/helpers/ketcher-page.mjs`）：Edge 写完
   `DevToolsActivePort` 后会短暂持有该文件句柄，紧接着的 `readFileSync` 间歇性抛 `EBUSY`
   （也可能读到尚未写完的内容）。同一棵树连续两次发布构建一次 7/7、一次 6/7。
   现把 `EBUSY/EPERM/EACCES/ENOENT` 当作"尚未就绪"继续轮询（仍受原有 15 秒预算约束），
   连续两次 `tests/browser` 均 7/7。

## 5. 下次升级 DSH 的复现步骤

```bash
# 1) 候选分支上改 harness.lock.json（cli + packages + recordedAt）与 runtime/versions.env
# 2) 重新安装并核对锁定
pnpm install --frozen-lockfile
node --test tests/integration/harness-compatibility.test.mjs
# 3) 更新 DSH_AGENT_LOOP_SHA256（补丁锚点基线）
node scripts/patch-dsh-runtime.mjs
# 4) 全量门禁
node --test "tests/unit/*.test.mjs" "tests/integration/*.test.mjs"
node scripts/regression/run.mjs
```

详细约定见 `docs/VERSIONING.md` §4。

## 6. 已知遗留与注意事项

- **子包的 npm `latest` tag 陈旧**（上游问题）：`@deepseek-ai/dsh-base` 的 `latest` 仍指向
  `0.0.1-rc.1`、`@deepseek-ai/dsh-agent-loop` 为 `0.1.0-rc.6`，只有 meta 包
  `@deepseek-ai/dsh` 的 `latest` 指向 `0.1.5-rc.1`。安装子包务必用精确版本或 `next`。
- **上游 git tag 交叉核验受限**：`api.github.com/repos/deepseek-ai/deepseek-harness` 匿名访问
  返回 403，本文"最新版本"的判据是 npm registry 的已发布版本，不含未发布/内部构建。
- **`docs/VERSIONING.md` 第 1 节仍写着 `0.1.1-rc.2`**，属历史文本，未随本次升级更新；
  权威版本以 `harness.lock.json` 与 `runtime/versions.env` 为准。
