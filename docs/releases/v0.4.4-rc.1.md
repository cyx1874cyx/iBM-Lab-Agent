# v0.4.4-rc.1 发布说明（候选版）

> 本候选版把 WebVPN 载体从「独立窗口贴靠」升级为「同窗子 WebView 真侧边栏」，
> 并修复此前「点打开 WebVPN 跳出纯白空窗」的两处根因。DSH 依赖未变更，仍为
> `0.1.5-rc.1`；DSH 兼容性结论沿用 `docs/releases/v0.4.3.md`。
>
> 与已打 tag 的 `v0.4.4`（旧「独立窗口」形态，人工验收待执行）不同，本 rc 是
> 真侧边栏形态的候选包，用于真机验收；验收通过后再决定转正版本号。

## 1. 本版本锁定

| 层 | 版本 | 记录位置 |
|---|---|---|
| iBM Lab Agent | `0.4.4-rc.1` | `package.json`、`desktop/package.json`、`desktop/src-tauri/Cargo.toml`、`desktop/src-tauri/tauri.conf.json`、`desktop/docs/release-manifest.json`、`runtime/versions.env`、`README.md` |
| DeepSeek Harness（CLI 与包集） | `0.1.5-rc.1` | `harness.lock.json`、`runtime/versions.env`（本版本未变更） |
| 运行时补丁基线 | `DSH_AGENT_LOOP_SHA256=257eb83c00a05ee068e9f4ba80ca71ab94e3a1275d24b7a0cf5038ff23dd0fd8` | `runtime/versions.env` |
| Windows 安装包 | 待打包（见 §4） | `desktop/.build/windows-release-<ts>/release-report.json` |

## 2. 本版本内容（WebVPN 同窗真侧边栏）

- WebVPN 载体改为 **Tauri 官方 `Window::add_child` 同窗子 WebView**，替代独立窗口贴靠；
  打开时主 WebView 自动收窄，WebVPN 占右侧 320–560 逻辑像素，收起后子 WebView 与专属
  profile 保留、登录态不丢；
- 修复「点打开 WebVPN 跳出纯白空窗」：① 建窗/操作窗口命令全部改 `async`（同步命令建窗
  在 Windows 上死锁）；② USTC 统一认证白名单补齐 `passport.ustc.edu.cn`（首跳 302 被拦成白页）；
- capability 从窗口级 `windows:["main"]` 收紧为 `webviews:["main"]`，外部 WebVPN 页面不继承 IPC；
- 捕获链路加固：新增 `Downloading` 态、新捕获替换旧 pending（修僵尸 pending 挡住重试）、
  上传失败保留 PDF 而非静默删除；
- 客户端阶段提示：捕获期间轮询会话状态，映射「加载出版社页 / 等待下载 / 归档中」并附 spinner；
- 顶栏新增 WebVPN 开关，按 `sidebarVisible` 同步按钮态。

## 3. 验证记录（源码门禁）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 客户端 bundle 一致性 | `node scripts/build-client.mjs`（重建幂等，无新增差异） | ✅ 通过 |
| 单元 + 集成 | `node --test "tests/unit/*.test.mjs" "tests/integration/*.test.mjs"` | ✅ **374 / 374 通过** |
| Rust | `cargo test`（`desktop/src-tauri`） | ✅ **89 passed / 0 failed / 1 ignored** |
| 版本一致性 | `node --test tests/unit/release-version-consistency.test.mjs` | ✅ 通过（位点均为 `0.4.4-rc.1`） |
| 空白/格式 | `git diff --check` | ✅ 无错误 |

## 4. 安装包验收

> 待打包。打包流水线 `desktop/scripts/build-windows-release.ps1 -ForcePrepare`，完成后回填
> 安装包文件名 / 字节数 / SHA256 / `publishable` / 包内 exe 版本 / 签名状态。

## 5. 安装包人工验收（待执行）

> 需在干净 Windows 用户/虚拟机上用**本轮 0.4.4-rc.1 安装包**完成，不得沿用旧版结论。

按 `docs/WEBVPN_0.4.3_PACKAGING_HANDOFF.md` §7 逐项记录，重点关注真侧边栏新路径：
- 安装/启动/DSH；
- 点「WebVPN」→ 侧边栏**嵌入主窗口右侧**（非独立弹窗、不白屏）；
- 主窗口拖动/缩放 → 侧边栏跟随；收起 → 主界面恢复全宽、登录态保留；
- WebVPN 首次登录与会话、真实 Nature/ACS/ScienceDirect PDF 与 SI 捕获；
- 跨重启会话保持、清除登录状态、失败恢复与 Edge 回退、安全检查。

> 未通过项必须记录复现步骤与阶段日志，禁止把未验收项写成已完成。
