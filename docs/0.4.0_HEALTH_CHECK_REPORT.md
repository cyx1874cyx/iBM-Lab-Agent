# iBM-Lab-Agent 0.4.0 现状体检报告（接手基线）

> **历史快照，不是当前验收结论**：本报告记录的是后续修复前的静态检查。当前状态必须重新运行测试与 Windows 发布门禁，并按 `0.4.x_THREE_RELEASE_AGENT_HANDOFF.md` 的三个完成书验收；Agent 不得引用本文的旧文件数、旧 manifest 或“未运行测试”等结论作为当前事实。

> 体检对象：`C:\Users\admin\Desktop\iBM-Agent\iBM-Agent-inCodeX`（分支 `codex/0.4.0-requirements`，HEAD `d4eb361`）
> 对照基线：`docs/0.4.0_IMPLEMENTATION_AND_PACKAGING_HANDOFF.md`（§3 已知问题、§8 DoD、§6 测试门禁）
> 体检方式：**全程只读**（git status/diff + 文件阅读 + 全仓 grep），未修改 inCodeX 任何文件，未运行测试
> 体检时间：2026-09-05

---

## 0. 结论摘要

0.4.0 的未提交改动（20 修改 + 13 新增文件，+289/−46）完成了**服务端契约与数据模型的地基**，但**客户端工作台三项核心改造几乎未落地**：三组件工作台仍以 CSS 隐藏旧区块冒充、`StepReactionLayout` 已定义但**未挂载**、路线总览仍为文字卡片。对照 §8 DoD 16 项：**1 项 ✅、5 项 ⚠️ 部分、9 项 ❌ 未完成、1 项不适用**。另有 1 个**发布阻断项未修**（release-manifest 仍为 0.3.2-rc.1）和 1 个**打包脚本与仓库现状断点**（pnpm workspace 迁移后 prepare-runtime 默认参数直接失败）。

| DoD 汇总  | 数量                                                            |
| ------- | ------------------------------------------------------------- |
| ✅ 完成    | 1（§DoD-16 不自动合 main）                                          |
| ⚠️ 部分完成 | 5（双源服务端、Ketcher 队列、锁定服务端、NMR 只读、AddStep 雏形）                   |
| ❌ 未完成   | 9（三组件真实实现、Ketcher 路线图、审核批次、模板 UI、两次点击、绘图登记、测试门禁、manifest、安装包） |
| 发布阻断    | 2（manifest 未 bump；prepare-runtime 默认断点）                       |

---

## 1. 仓库基线核对

| 检查项              | 交接文档记载                     | 实测                                                                                              | 一致       |
| ---------------- | -------------------------- | ----------------------------------------------------------------------------------------------- | -------- |
| 工作目录             | inCodeX                    | ✅ 一致                                                                                            | ✅        |
| 分支               | `codex/0.4.0-requirements` | ✅                                                                                               | ✅        |
| HEAD             | `d4eb361`                  | ✅（与 release-0.3.0 同位）                                                                           | ✅        |
| 源码版本             | 0.4.0-rc.3                 | package.json / desktop/package.json / Cargo.toml / Cargo.lock / tauri.conf.json 5 处均 0.4.0-rc.3 | ✅        |
| release-manifest | 0.3.2-rc.1（阻断项）            | `desktop/docs/release-manifest.json` 仍 `ibmLabAgent: 0.3.2-rc.1`，**未修**                         | ⚠️ 阻断仍存在 |
| 工作树改动            | "大量未提交尝试"                  | 20 M + 13 ??（详见 §2）                                                                             | ✅        |

---

## 2. 变更总览（git status 全量）

**修改（20）**：`client/index.js`(+106)、`lib/synthesis.js`(+42)、`lib/remote.js`(+26)、`cordis.patch.yml`(+9)、`package.json`(+5)、`src/synthesis/models.js`(+18)、`src/nmr/models.js`(+9)、`src/chemistry/rdkit-pubchem.js`(+3)、`scripts/ketcher-shell/src/main.tsx`(+1 实质行)、`client/assets/ketcher-standalone/index.html`(+1)、`client/assets/ketcher-standalone/assets/index-DZ5BIR34.css`（**仅行尾噪声，diff 为空**）、desktop 版本 4 文件、`desktop/scripts/prepare-runtime.ps1`(+70)、`desktop/scripts/verify-package.ps1`(+26)、测试 4 文件（boot-lite/manual-capture/remote/task-flow，仅注入列表加 `labExperimentPlanTemplates`）。

**新增（13）**：`src/synthesis/compound-resolve.js`、`lib/experiment-plan-templates.js`、`src/experiment-plan-template.js`、`tests/unit/compound-resolve.test.mjs`、ketcher 新哈希块 ×3、`docs/0.4.0_*.md` ×2、`pnpm-lock.yaml` ×3、`scripts/ketcher-shell/pnpm-workspace.yaml`。

---

## 3. §8 DoD 逐项对照

### ❌ 1. 三个全宽主组件真实实现（非 CSS 隐藏模拟）— **未完成（最关键差距）**

- `StepReactionLayout` 在 `client/index.js:545` 定义，**全文件仅此一处出现，无任何渲染调用点** → 交接文档 §3.1 描述的"已定义但未挂载"核心缺陷**原样存在**。
- 新增 CSS（`client/index.js:69`）仍是大段隐藏规则：`.sw-metrics, .sw-analy-block, .sw04-analysis, .sw04-plan, .sw-cond{display:none}`。
- **为让隐藏生效还给旧区块贴了新 class**：`index.js:1017` 给"分析与决策"段加 `sw04-analysis`、`index.js:1037` 给"实验计划"段加 `sw04-plan` —— 恰好命中隐藏规则。这正是交接文档 §2.2 明令禁止的"仅靠 CSS 隐藏旧组件冒充完成"。
- 实际可见布局 = 旧结构 + 隐藏：路线卡片（sw-graph 区）→ sw-grid（左"实验条件"section 内 `.sw-cond` 表格被整体隐藏、右"文献来源/Evidence"可见）→ 被隐藏的分析/计划段。
- 依赖该缺陷的后果：**难度说明段落（`.sw04-difficulty`，index.js:1025）与"生成计划草案"按钮（1042）位于被隐藏段内，用户实际看不到**；条件表、结构卡随 `.sw-cond` 一并隐藏。

### ❌ 2. 路线总览为 Ketcher 结构图路线 — **未完成**

- `sw-graph`（index.js:918–945）仍是文字按钮步骤卡：step-id / reaction / "原料 + … → 产物 + …" / Evidence chip / 审核 chip。无任何 Ketcher 化合物节点 + 反应箭头的路线图渲染逻辑（全仓无 routeGraph 类函数）。

### ⚠️ 3. 第二组件横向反应式 / CAS / 难点 / 计划按钮可见 — **部分**

- ❌ `StepReactionLayout` 组件（`index.js:545-556`，含 `.sw04-reaction` 三列 CSS）写好但**不渲染**。
- ⚠️ CAS 显示：`StructureCard` 增加 CAS chip + "CAS 待确认"兜底（index.js diff）；模型 `stepStructureSchema` 增 `casNumber/inchiKey`（models.js:139+）。显示层 OK。
- ⚠️ 难点：模型增 `difficultySummary`（max 50 字，routeStepSchema）；UI 段落已写但位于被隐藏段（见上）。
- ❌ 实验计划按钮：仅顶部工具栏一键"生成实验计划草案"（index.js:916，调 `synth_plan_from_route`），无两次点击状态机。

### ❌ 4. 第三组件事实项—截图联动 + 分轮审核 — **未完成**

- 截图联动（EvidenceShot）与单条确认/无法确认按钮（0.3.2 已有）仍可见可用；按钮文案"驳回"→"无法确认"（index.js:1005）。
- 全仓 grep `reviewBatch / review_batch / reviewRound / originalExtract / userCorrection / artifactRequest` **零命中** → Evidence 无轮次/修正值扩展、无审核批次服务、无 Agent 批量更新门禁（WP1.1–1.2、WP4 全缺）。

### ⚠️ 5. PubChem/CACTUS 双源流程 — **服务端/模型完成，UI 未接入**

- ✅ `src/synthesis/compound-resolve.js`：dual-confirmed / single-source / conflict / unresolved 四态 + CAS_RE + 可注入 fetch（测试友好）。
- ✅ 远程口 `synth_compound_resolve_dual` 已注册（lib/remote.js、client descriptors index.js:87）。
- ✅ `rdkit-pubchem.js` lookupPubChem 增 InChIKey；`lib/synthesis.js` resolveStepStructures 写入 `verification:{status:"single-source"}`。
- ❌ **UI 的"解析缺结构"仍调用 PubChem-only 的 `synth_step_resolve_smiles`**（index.js:868–874），`synth_compound_resolve_dual` **无任何调用点** → 双源一致才登记、冲突只展示候选、单源标注来源的界面逻辑均未实现。
- ⚠️ 测试 `compound-resolve.test.mjs` 仅 3 例（dual-confirmed / conflict / CAS 格式），缺 §6.2 要求的单源与双失败路径。

### ⚠️ 6. Ketcher 开发/离线/失败恢复 — **队列加固为主，未整体验收**

- ✅ 渲染队列：job 可取消（cancelled）、ready 超时不再伪造 ready 而是取消任务并 resolve(null)（index.js:396–440）；postMessage 目标从 `"*"` 收紧为 `location.origin`（client 与 shell main.tsx 双端）——符合 WP2.1 来源校验方向。
- ❌ §WP2.7 未做：`client/assets/ketcher-standalone/assets/` 残留 3 个旧哈希块（`index-C8ON9N3L.js` 29.2MB、`index.modern-55d8e3ef-c0gldBNN.js`、`index.modern-ApFbIqWy.js`），新 index.html 只引 `index-CAedvWGz.js` → 未清除死资源（约 30MB）。
- ❌ "10 结构并发 / 非法 SMILES 恢复 / 尺寸缓存隔离 / 断网离线"专项验收未跑（§6.3）。
- ✅ shell 产物与 client assets 已同步（dist/assets 为新 4 块）。

### ⚠️ 7. 人工建路线 / 步骤 CRUD+排序 / 版本锁 — **部分（锁定雏形，CRUD 残缺）**

- ✅ 锁定闭环雏形：UI "锁定版本"按钮（index.js:901）+ `synth_route_lock` 远程 + 服务端 `lockRoute`（有 pending Evidence 时拒绝，lib/synthesis.js）+ 锁后按钮区变为"已锁定"chip；`createRouteRevision` 复制版本显式 `locked:false`。
- ⚠️ `add-step`（index.js:782 起）用**连续 3 个 `window.prompt`**（反应/反应物/产物）——交接文档 WP5 明确禁止（"使用页面表单或抽屉"），且无编辑/删除/排序 UI。
- ❌ 新建路线仍需 Agent 对话登记（空状态仅改提示文案），无"新建路线"入口。

### ⚠️ 8. 写保护语义 — **服务端锁已加，但与需求语义不一致**

- ⚠️ `assertWritable`（lib/synthesis.js:122-127）仍是**双门禁**：`locked` 抛错 **且** `status !== "draft"` 抛错。需求（§3.4）要求锁定与审核状态独立：`locked=false` 即可修改。当前非 draft 未锁定路线仍被拒 → §6.2 测试"未锁定且非 draft 仍可按需求修改"**必然失败**。
- ✅ 锁定拒绝已覆盖所有写入口：addRouteStep / updateRouteStep / resolveStepStructures / setStepStructure（UI 侧 openStructureEditor / resolveStepSmiles 同步加 locked 判断）。
- ❌ "已人工确认事实不被 Agent 回写覆盖 / Agent 回写进下一轮"：无对应机制（依赖 WP4，未实现）。
- ❌ 无锁定语义自动化测试（tests/regression/cases/synthesis.mjs 中 "locked" 仅 CAS 授权词汇，无 route lock 用例）。

### ❌ 9. 实验计划模板 UI + 任务保存模板快照 — **服务端完整，消费侧全缺**

- ✅ 服务端五件套基本齐：`src/experiment-plan-template.js`（schema/默认模板）+ `lib/experiment-plan-templates.js`（domain/service，list/resolve/create/update/copy/archive + 版本快照不可变 + seed）+ package.json exports + cordis.patch.yml 注册 + lib/remote.js 注入 + boot-lite/测试注入。
- ❌ **lib/tasks.js 全文件无 `experimentPlanTemplates` 引用** —— cordis.patch.yml/remote/boot 给它注入的 `labExperimentPlanTemplates` 处于空转，生成实验计划并未读取模板、不保存模板快照。
- ❌ 模板管理 UI（第三页签）不存在；client descriptors 虽注册 6 个 `experiment_plan_templates_*` 但无 UI 调用。
- ❌ verify-package.ps1 的 import 探针未含 experiment-plan-templates（§7.4 建议项）。

### ❌ 10. 实验计划/精读/PPT 两次点击 + 另存 — **未开始**

- `plan` 动作（index.js:777-781）仍是单击即 `synth_plan_from_route` 生成内部草案。无 idle/confirm-send/requested/ready/stale/failed 状态机、无 DOCX 请求化、无"完成后另存"。

### ⚠️ 11. NMR / 绘图登记 — **NMR 半程，绘图登记全缺**

- ✅ NMR：模型增 `entityId/compound{name,casNumber,smiles}/deuteratedSolvent`（旧数据 optional）；characterization 页换用新 `NmrRegistry`（结构图/名称/CAS/氘代试剂主信息，缺 SMILES 显示"结构待补充"）。
- ❌ NMR 登记/编辑表单仍无（无 nmr_create 触发的 UI 流程）。
- ❌ 绘图登记：全仓 grep `plotRecord / plot_record` 零命中，无模型/服务/远程/UI。

### ❌ 12. 源码测试门禁 — **未运行且 §6.2 新增测试缺失**

- 工作树无任何测试运行记录；无法确认 `test:unit / test:integration / regression / lint / build:client / check:preset-exports` 当前是否通过（**接手第一动作应先跑一遍**）。
- §6.2 要求的新增测试（未锁定非 draft 可改、锁定拒绝全部写接口、Evidence 轮次/覆盖保护、双源四条路径、三组件结构、两次点击恰好一次请求、模板快照不可变等）**除 compound-resolve 3 例外全部缺失**。

### ❌ 13. 版本字段 + release manifest 一致 — **阻断**

- 源码 5 处 = 0.4.0-rc.3 ✅；`release-manifest.json` `ibmLabAgent` = **0.3.2-rc.1** ❌（交接文档 §2.1 明确为发布阻断项，未修）。其余 manifest 条目（dsh 0.1.1-rc.2 / python 3.11 / node 24.16.0 / originMcp 0.1.4 / mnovaMcp 0.3.1 / mcpSdk 1.29.0）与 pitfalls §13 记录一致。

### ❌ 14/15. NSIS 安装包验证 + 干净环境验收 — **无产物**

- 未见新安装包；`desktop/src-tauri/target/release/bundle/nsis` 未检查（体检不产生产物）。

### ✅ 16. 不自动合并 main — **满足**

- 分支 `codex/0.4.0-requirements` 独立；未打 tag、未合 main（`main` 仍停 0.1.16 量级历史版本）。

---

## 4. §3 已知问题逐条确认（vs 文档记载）

| 条目                   | 文档记载                             | 实测结论                                                               |
| -------------------- | -------------------------------- | ------------------------------------------------------------------ |
| §3.1 工作台"定义但未挂载"     | StepReactionLayout 未挂载、CSS 隐藏旧组件 | **完全仍存在**：无调用点 + 隐藏规则 + 给旧段贴 class 以命中隐藏                           |
| §3.2 路线总览是文字卡片       | 应改 Ketcher 结构图                   | **仍为文字卡片**，无路线图渲染                                                  |
| §3.3 双源解析未接入界面       | 应接入 UI                           | **仍为 PubChem-only**；dual 远程口空转                                     |
| §3.4 写保护与需求不一致       | assertWritable 双门禁               | **仍为双门禁**（locked + draft）；locked 门禁已补但 draft 限制未按需求解除              |
| §3.5 模板/产物/登记只完成局部后端 | 模板有后端无 UI 等                      | 模板服务端更完整了（含五件套大半），但 **tasks 未消费模板**、产物状态机/绘图登记仍缺；NMR 加了只读 registry |

---

## 5. 体检发现的额外事项（交接文档未记载）

### 5.1 ⚠️ 依赖管理已 pnpm workspace 化，prepare-runtime 存在默认参数断点

- **现状**：根 `node_modules` 为 pnpm 布局（`.pnpm` 存在；直接依赖仅 4 个生产包 + `@deepseek-ai/*` 全套 DSH 生态 160+ 包；`dsh-lab-agent` 自链接到仓库根）。**`runtime/launcher/node_modules` 已不存在**（该目录只剩 package.json/pnpm-lock.yaml/launcher/install-ubuntu.sh 等 Linux 部署件）。
- **后果**：`prepare-runtime.ps1` 已为 pnpm 重写依赖复制（`Resolve-MaterializedPackageDirectory` 解析链接 → .pnpm 真实目录，`/XJ` 排除 junction，显式物化 fast-xml-parser/js-yaml/jszip/zod 及其传递闭包）并新增 `-DshSource` 参数，但**默认值仍是 `runtime\launcher\node_modules` → 该路径不存在时直接 throw "DeepSeek Harness payload was not found"**。按交接文档 §7.4 的原始命令运行将**立即失败**，必须显式传 `-DshSource <根>\node_modules`。
- **连带**：交接文档 §7.3 的 `npm ci` 安装命令与 pnpm workspace 布局不匹配，需要按新依赖管理改写（根目录已有 `pnpm-lock.yaml` 且 untracked）。
- 该迁移与 pitfalls 文档 §10 记录的"runtime/launcher 内 pnpm 残留导致 makensis 失败"为同类问题的不同阶段——迁移本身是对的方向，但 prepare-runtime 调用约定与文档需要同步更新。

### 5.2 Ketcher 旧哈希块残留（§WP2.7 未做）

`client/assets/ketcher-standalone/assets/` 共存 8 文件：新 `index-CAedvWGz.js`(29.0MB) + 旧 `index-C8ON9N3L.js`(29.2MB) + 新旧 `index.modern-*` 各 2 个。index.html 只引新块 → 死资源约 30MB 随包分发（NSIS 体积 +44k 文件树多 3 条目）。

### 5.3 verify-package.ps1 已扩展但未覆盖 0.4.0 新增面

✅ 新增 4 个生产依赖的 `package.json` 存在性检查 + `.package-import-probe.mjs`（fast-xml-parser/js-yaml/jszip/zod + remote/tasks/ppt-templates/manual-capture 导入探针，呼应 pitfalls §10 教训）。❌ 未按 §7.4 建议补：Ketcher index.html 及哈希资源存在性、experiment-plan-templates 服务导入、只读合成接口 Web 冒烟。

### 5.4 其他

- `index-DZ5BIR34.css` 的 M 为 CRLF 行尾噪声（`git diff` 内容为空），提交时注意勿产生空改动。
- `StructureCard` CAS 显示依赖 `entry.casNumber`，旧数据缺省时正确显示"CAS 待确认"——显示层已兼容旧数据 ✅。
- ketcher-shell 构建产物已同步到 client assets（dist 与新 assets 4 块一致），但 VERSION 文件内容未核实是否随重建更新。

---

## 6. 接手建议（按优先级）

### P0 — 发布阻断（必须先做）


1. `release-manifest.json` bump 到 0.4.0-rc.3，并核对 dsh/node/python/MCP 条目与打包实况。
2. 明确 pnpm workspace 后的打包命令基线：`prepare-runtime.ps1 -DshSource <根>\node_modules`，并把交接文档 §7.3/§7.4 命令改写为 pnpm 等价（`pnpm install` / `corepack pnpm`），补一次 `verify-package -WebSmokeTest` 基线。

### P1 — 0.4.0 核心未完成项（决定是否继续 rc.4 前必须决策）

1. 三组件工作台：删除"为隐藏贴 class + display:none"的掩盖做法，真实渲染 `RouteOverviewPanel / StepDetailPanel / FactReviewPanel`，把 `StepReactionLayout`（已写好）挂载到第二组件。
2. 路线级 Ketcher：决定完整路线图渲染本期是否交付；若按交接文档 §1 的收敛范围可缓，至少要给出步骤选择 + 结构预览的真实第二组件。
3. 双源 UI：解析缺结构切到 `synth_compound_resolve_dual`，实现一致/单源/冲突/双败四态交互与写入门禁。
4. `assertWritable` 语义：按需求去掉 `status !== "draft"` 限制（若产品确认非 draft 未锁定确实可改），否则明确记录为有意保留并在 DoD 划掉对应测试。
5. add-step 用表单/抽屉替换 `window.prompt`；补步骤编辑/删除/排序（或明确延期）。完善 p3,

### P2 — 服务端已有资源的消费与补齐

1. `lib/tasks.js` 消费 `labExperimentPlanTemplates`（或移除空注入）；模板管理 UI（第三页签）。
2. Evidence 轮次/审核批次/Agent 回写门禁（WP4）与绘图登记（WP7）——若本期不做，需产品确认砍项。
3. 产物两次点击状态机（WP6）——需与"实验计划模板"统一。

### P3 — 工程清理与验证

1. 清除 ketcher-standalone 旧哈希块（30MB）；重建后验证 index.html 引用集 = 目录文件集。
2. verify-package.ps1 按 §7.4 补 Ketcher/新服务/只读合成冒烟。
3. 先跑源码门禁全量（build:client / check:preset-exports / test:unit / test:integration / regression / lint），记录基线后再动代码。

> 注意：本报告所有结论均基于未运行测试的静态核查；上表"❌/⚠️"表示代码层面缺失或与需求不一致，不代表功能运行表现。
